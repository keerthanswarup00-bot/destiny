node_modules
.next
.env
.env.*
!.env.example
.env.example.*
*.log
.DS_Store
tsconfig.tsbuildinfo
.vercel
.env*
supabase/.temp/

*.bak

# Long-form films. GitHub rejects any file over 100MB, so the two big masters stay
# out of git and are meant to be served from R2. The vertical Reels are all well
# under the limit and ship with the site.
public/videos/*
!public/videos/reel-*.mp4
