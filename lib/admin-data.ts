import { createSupabaseAdminClient } from "@/lib/supabase/admin";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export async function adminDb() { return process.env.SUPABASE_SERVICE_ROLE_KEY ? createSupabaseAdminClient() : createSupabaseServerClient(); }
export async function dashboardCounts() { const db = await adminDb(); const [clients, galleries, photos, selections, views] = await Promise.all([db.from("clients").select("id", { count: "exact", head: true }), db.from("galleries").select("id", { count: "exact", head: true }).eq("status", "published"), db.from("photos").select("id", { count: "exact", head: true }), db.from("selections").select("id", { count: "exact", head: true }), db.from("gallery_views").select("view_count")]); const viewsTotal = (views.data ?? []).reduce((sum, row) => sum + (Number(row.view_count) || 0), 0); return [clients.count ?? 0, galleries.count ?? 0, photos.count ?? 0, selections.count ?? 0, viewsTotal]; }